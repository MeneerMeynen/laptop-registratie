# Local development (outside Docker). When running via Docker Compose the
# DATABASE_URL is overridden in docker/compose.yaml to use the 'db' service host.
DATABASE_URL=mysql+pymysql://root:***REMOVED***@localhost:3306/laptops
DEBUG=false
