__pycache__/
*.pyc
*.egg-info/
.DS_Store
.claude/
# TLS certificates (generated locally via mkcert — not committed)
certs/
# Local environment file with secrets — never commit. Use .env.example as template.
.env
cert_setup.sh
